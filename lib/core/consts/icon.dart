import 'package:flutter/material.dart';

class appicon {
  static const home_icon = Icon(
    Icons.search,
  );
  static const Icon cart_icon = Icon(
    Icons.shopping_cart,
    size: 23,
  );
  static const Icon close_icon = Icon(
    Icons.close,
    size: 25,
  );
//  static Icon home_icon = Icon(Icons.search);
}
